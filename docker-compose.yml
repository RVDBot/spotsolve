services:
  spotsolve:
    image: ghcr.io/rvdbot/spotsolve:latest
    ports:
      - "3031:3000"
    volumes:
      - ./data:/app/data
    environment:
      - DATA_FILE=/app/data/settings.json
    restart: unless-stopped
